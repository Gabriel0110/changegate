{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Gabriel0110/changegate/schemas/changegate-graph.schema.json",
  "title": "ChangeGate Blast-Radius Graph v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "nodes", "edges"],
  "properties": {
    "version": { "const": 2 },
    "nodes": {
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/node" }
    },
    "edges": {
      "type": "array",
      "items": { "$ref": "#/$defs/edge" }
    }
  },
  "$defs": {
    "node": {
      "type": "object",
      "additionalProperties": true,
      "required": ["id", "address", "type", "kind", "name"],
      "properties": {
        "id": { "type": "string" },
        "address": { "type": "string" },
        "type": { "type": "string" },
        "kind": {
          "type": "string",
          "enum": [
            "public_entrypoint",
            "workload",
            "data_store",
            "secret",
            "kms_key",
            "principal",
            "policy",
            "network_boundary",
            "unknown"
          ]
        },
        "name": { "type": "string" },
        "provider": { "type": "string" },
        "module_path": { "type": "array", "items": { "type": "string" } },
        "environment": { "type": "string" },
        "tags": { "type": "object", "additionalProperties": { "type": "string" } },
        "values": { "type": "object" },
        "changed": { "type": "boolean" },
        "actions": { "type": "array", "items": { "type": "string" } },
        "synthetic": { "type": "boolean" }
      }
    },
    "edge": {
      "type": "object",
      "additionalProperties": false,
      "required": ["from", "to", "type"],
      "properties": {
        "from": { "type": "string" },
        "to": { "type": "string" },
        "type": {
          "type": "string",
          "enum": [
            "depends_on",
            "routes_to",
            "allows_ingress",
            "allows_egress",
            "can_assume",
            "can_pass_role",
            "can_read_data",
            "can_write_data",
            "attached_to",
            "contained_in",
            "has_public_access",
            "grants_permission",
            "reads_secret",
            "encrypts_with",
            "writes_to",
            "replicates_to",
            "protects",
            "invokes"
          ]
        },
        "source": {
          "type": "string",
          "enum": ["plan", "cloud_context", "inferred", "mixed"]
        },
        "confidence": {
          "type": "string",
          "enum": ["low", "medium", "high"]
        },
        "evidence": { "type": "array" },
        "metadata": { "type": "object", "additionalProperties": { "type": "string" } }
      }
    }
  }
}
